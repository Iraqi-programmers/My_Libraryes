<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="Border" x:Key="Custom3DCardStyle_1">
        <Setter Property="Width" Value="220"/>
        <Setter Property="Height" Value="120"/>
        <Setter Property="CornerRadius" Value="15"/>
        <Setter Property="Padding" Value="15"/>
        <Setter Property="Margin" Value="17.5"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="7.5" Opacity="0.5"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ستايل البطاقات مع الظل -->
    <!--<Style TargetType="Border" x:Key="Custom3DCardStyle">
        <Setter Property="Width" Value="220"/>
        <Setter Property="Height" Value="120"/>
        <Setter Property="CornerRadius" Value="15"/>
        <Setter Property="Padding" Value="15"/>
        <Setter Property="Margin" Value="17.5"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="7.5" Opacity="0.5"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                        To="LightBlue" Duration="0:0:0.3"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                        To="White" Duration="0:0:0.3"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>-->

</ResourceDictionary>